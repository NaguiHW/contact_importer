<% provide(:title, "Importer") %>

<h1>Welcome <%= current_user.email %></h1>

<%= form_with(url: importers_path, model: @csv, local: true) do |f| %>
  <%= f.file_field :file, required: true %>
  <%= f.submit "Upload CSV" %>
<% end %>

<%= link_to "Log out", logout_path, method: :delete %>